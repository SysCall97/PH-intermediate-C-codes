#include<stdio.h>
#include<conio.h> // unnecessary header file for codeblocks

int main() {
    int a, b, c, x;
    // problem 1
    int equation1 = (a*x*x) + (b*x) + c;
    int equation2 = ((a*x*x)/(b*x)) + c;
    return 0;
}
